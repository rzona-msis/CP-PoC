# Campus Resource Hub - Docker Environment Variables
# Copy this to .env for Docker deployment

# ============================================================
# Flask Configuration
# ============================================================
SECRET_KEY=your-secret-key-here-change-in-production-min-32-chars
FLASK_ENV=production

# ============================================================
# Email Configuration (Optional)
# ============================================================
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=true
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-gmail-app-password
MAIL_DEFAULT_SENDER=noreply@campushub.edu
MAIL_SUPPRESS_SEND=false

# ============================================================
# Google Calendar API Integration (Optional)
# For auto-syncing bookings to user's Google Calendar
# ============================================================
# Get these from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret-here
# IMPORTANT: Use port 8080 for Docker (not 5000)
GOOGLE_REDIRECT_URI=http://localhost:8080/calendar/callback

# ============================================================
# Google Gemini AI (Optional)
# For AI Assistant chatbot feature
# ============================================================
# Get API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# ============================================================
# Google Analytics (Optional)
# For web analytics tracking
# ============================================================
# Get from: https://analytics.google.com/
GA_MEASUREMENT_ID=G-XXXXXXXXXX

# ============================================================
# Google Cloud Platform (Optional)
# For advanced analytics with BigQuery integration
# ============================================================
GOOGLE_CLOUD_PROJECT=your-gcp-project-id
# Path inside Docker container (mount credentials file as volume)
GOOGLE_APPLICATION_CREDENTIALS=/app/credentials/gcp-credentials.json

# ============================================================
# Database Configuration
# ============================================================
# SQLite - handled automatically by Docker
DATABASE_PATH=/app/data/campus_hub.db

# ============================================================
# PRODUCTION NOTES:
# ============================================================
# 1. For production, update GOOGLE_REDIRECT_URI to your domain:
#    GOOGLE_REDIRECT_URI=https://your-domain.com/calendar/callback
#
# 2. Update redirect URI in Google Cloud Console to match
#
# 3. Generate a strong SECRET_KEY:
#    python -c "import secrets; print(secrets.token_hex(32))"
#
# 4. For GCP credentials, mount the JSON file:
#    volumes:
#      - ./path/to/gcp-credentials.json:/app/credentials/gcp-credentials.json:ro
#
# ============================================================

